# TODO

## 백로그

페이지 헤더? 토플이 여전히 있음.

### 인프라

- [ ] **S3 백업** — `data/` 디렉토리를 S3에 스냅샷 저장
  - 매일: `data/meta.json`, `data/settings.json`, `data/stt/`
  - 매주: `data/audio/` (용량 크고, 변경 빈도 낮음)
  - S3에서 EC2로 복원하는 스크립트
- [ ] **Docker 배포** — 컨테이너화로 배포 간소화
  - 단일 `Dockerfile` (Node + 빌드)
  - `docker-compose.yml`에서 `data/` 볼륨 마운트
  - EC2 외 아무 VPS에서도 배포 가능
- [ ] **도메인 + HTTPS** — 프로덕션 수준 접근
  - 도메인 등록 또는 서브도메인 사용
  - certbot + nginx로 Let's Encrypt 인증서
  - 자동 갱신 cron
- [ ] **CI/CD** — GitHub Actions 파이프라인
  - main 푸시 → EC2 SSH → pull + build + restart

### 멀티유저 지원

- [ ] **인증** — Basic Auth 대신 유저 계정 시스템
  - JWT 또는 세션 기반 인증
  - 로그인 / 회원가입 페이지
- [ ] **유저별 데이터 격리** — 각 유저가 자기 라이브러리만 볼 수 있도록
  - `data/users/{userId}/` 디렉토리 구조
  - 유저별 meta.json, audio, STT 결과 분리
  - 언어 설정도 유저별로
- [ ] **유저 관리** — 관리자 기능
  - 초대 전용 가입 또는 오픈 가입 토글
  - 유저별 저장 용량 제한

### STT 품질

- [ ] **긴 문장 분할** — 15초 이상 세그먼트를 자연스러운 pause 지점에서 분할
  - word timestamps 사이 gap을 분할 지점으로 활용
  - 쉼표 등 구두점을 보조 분할 힌트로 사용
- [ ] **짧은 문장 병합** — 2초 미만 세그먼트를 인접 세그먼트와 병합
  - "OK", "Yes", "Yeah" 등 극히 짧은 문장 대상

### 기능

- [ ] **학습 모드** — 딕테이션, 쉐도잉
- [ ] **학습 통계** — 일일 청취 시간, 학습한 문장 수 추적
- [ ] **데이터 내보내기/가져오기** — 유저 데이터 백업 및 복원
